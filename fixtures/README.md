# Fixtures

This directory is reserved for synthetic or explicitly approved public-safe save fixtures.

Do not commit real local Gothic Remake saves by default. The Rust test suite builds minimal synthetic GSAV containers in memory for parser and writer coverage.
