[package]
name = "gore_core"
version = "0.1.0"
edition = "2021"
description = "Shared reflection/catalog model for gore-tools modding utilities."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
