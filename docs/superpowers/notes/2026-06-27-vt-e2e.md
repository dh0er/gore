# VT preview + replace — in-game validation (2026-06-27)

Status: **boot-clean confirmed; visual render check is user-driven** (automated control declined).

## Built + validated offline (Tasks 1–5)
- `vt::parse`/`serialize` — byte-faithful round-trip on the real Biter VT (`.uexp` region byte-identical).
- `vt::decode_layer0` — decodes the Biter VT (4096² PF_DXT1) to a clean flat image (no seams) → VT **preview** works through `texture_extract`/the tab.
- `vt::retile` + `replace_texture_image` VT routing — re-tile a same-dims single-layer image → repack → readback decodes to the exact test image (4096² magenta, R0/G0/B0 deviation). Same-dims chunk-length invariant matched on the real VT.

## In-game test mod (deployed)
`~mods\zzz_VtTest_P.{utoc,ucas,pak}` — two re-tiled VTs, each magenta + thick cyan diagonal stripes (so tile seams would show as broken/offset stripes):
- `/Game/Assets/Characters/Humans/Main/NHero/Model/Textures/T_NH_Ch1_Clothes_D` (2048² PF_DXT5) — the **player's chapter-1 outfit, visible from game start**.
- `/Game/Assets/Characters/Creatures/Biter/Model/Armor/Textures/T_Biter_Armor_D` (4096² PF_DXT1) — structural test (large, multi-chunk).

## Result so far
- **Boot: CLEAN.** Launched `G1R-Win64-Shipping.exe` windowed with the mod deployed; reached the menu, no crash, no new crash dir (baseline `EB81BA07…` 6/26 10:33 unchanged). → the re-tiled VT package metadata is accepted at IoStore mount/startup.

## HUMAN ACTION (final visual check)
Start a New Game; once the Nameless Hero is on screen, look at his clothes:
- **Magenta with clean, continuous cyan diagonal stripes** → VT replace fully proven (border rule + SHA + offset tables all correct).
- **Broken/offset stripes at tile boundaries** → border-replication rule wrong (clamp vs mirror) — fix `vt::retile` border code.
- **Crash on level load** (new crash dir, check `CrashContext.runtime-xml`) → SHA-1 hash validation or an offset/size table mismatch — fix the flagged item.

Undeploy after: `gore texture undeploy --game "D:\SteamLibrary\steamapps\common\Gothic 1 Remake" --name zzz_VtTest_P`.

Open risks being validated here: border rule (clamp-to-edge, assumed), per-chunk FSHAHash (SHA-1, assumed; engine may or may not validate).
