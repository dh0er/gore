[package]
name = "gore-oodle"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# Enables the std::error::Error impl on Error. Off by default (crate is no_std).
std = []

[dev-dependencies]
base64 = "0.22"
sha1 = "0.10"
hex = "0.4"
