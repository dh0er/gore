#pragma once

// UNREANGEL's public header includes the plugin's generated native-caller
// helpers, but the AngelScript core itself does not use them. Native bindings
// will arrive through the future G1R compiler profile, never this shim.
