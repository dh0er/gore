[package]
name = "gore-story-build"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic, non-publishing story build plans for GORE Mod Studio."

[dependencies]
gore-authoring = { path = "../gore-authoring" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
